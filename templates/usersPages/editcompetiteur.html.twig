{% extends 'usersBase.html.twig' %}

{% block body %}

<div class="container mt-4">
    <h2 class="mb-4">Mettre à jour le profil de compétition</h2>

    {{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': ''}}) }}

    <div class="form-row mb-3">
        <div class="col-md-6">
            <div class="form-group">
                {{ form_label(form.ceinture, null, {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.ceinture, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
    </div>

    <div class="form-row mb-3">
        <div class="col-md-6">
            <div class="form-group">
                {{ form_label(form.kimono, null, {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.kimono, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
    </div>

    <div class="form-row mb-3">
        <div class="col-md-6">
            <div class="form-group">
                {{ form_label(form.categoriePoid, null, {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.categoriePoid, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
    </div>



      <div class="form-row mb-3">
        <div class="col-md-6">
            <div class="form-group">
                {{ form_label(form.categoriePoidGI, null, {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.categoriePoidGI, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
    </div>

    

    

    <div class="form-row mb-4">
        <div class="col-md-6">
            <button type="submit" class="btn btn-primary">Valider</button>
        </div>
    </div>

    {{ form_end(form) }}

</div>



{% endblock %}

